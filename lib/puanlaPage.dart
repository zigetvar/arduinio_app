import 'package:flutter/material.dart';

class PuanlaPage extends StatelessWidget {
  const PuanlaPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}