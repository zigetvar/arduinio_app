import 'package:flutter/material.dart';

class PaylasPage extends StatelessWidget {
  const PaylasPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}